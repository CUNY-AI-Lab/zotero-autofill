# To get started with Dependabot version updates, you'll need to specify which
# package ecosystems to update and where the package manifests are located.
# Please see the documentation for all configuration options:
# https://docs.github.com/github/administering-a-repository/configuration-options-for-dependency-updates

version: 2
updates:
  - package-ecosystem: "npm" # See documentation for possible values
    directory: "/" # Location of package manifests
    schedule:
      interval: "weekly"
    groups:
      all-non-major:
        update-types:
          - "minor"
          - "patch"
    ignore:
      # 5.1.1+ removed ztoolkit.Menu and Zotero 7 support; the plugin still
      # supports Zotero 7 (strict_min_version 6.999). Lift this when moving the
      # menus to Zotero.MenuManager and dropping Zotero 7.
      - dependency-name: "zotero-plugin-toolkit"
        versions: [">=5.1.1"]
